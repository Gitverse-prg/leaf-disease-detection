# version_check.py
import sys
print("Python version:", sys.version)

{
    "version": 2,
    "builds": [
      { 
        "src": "./index.py", 
        "use": "@vercel/python",
        "config": {"runtime":"python3.8"} 
      }
    ],
    "routes": [
      { 
        "src": "/(.*)", 
        "dest": "/" }
    ]
  }
